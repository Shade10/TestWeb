@model WebTest.Models.GuestResponse

@{
    ViewBag.Title = "Thanks";
}

@{ 
    try
    {
        WebMail.SmtpServer = "smtp.gmail.com";
        WebMail.SmtpPort = 587;
        WebMail.EnableSsl = true;
        WebMail.UserName = "dsffa";
        WebMail.Password = "asdaa";
        WebMail.From = "sfeee";

        WebMail.Send("sdsfas@dsaf.com", "Powiadomienie RSVP", Model.Name + ((Model.WillAttend ?? false) ? "" : "nie") + "przyjdzie");
    }
    catch (Exception)
    {
        @:<b>Przepraszamy - nie możemy wysłać wiadomości RSVP</b>
    }
}

<div class="text-center">
    <h1>Dzięki, @Model.Name!</h1>
    <div class="lead">
        @if (Model.WillAttend == true)
        {
            @:Cieszymy się niezmiernie
        }
        else
        {
            @:Przykro nam :(
        }
    </div>
</div>